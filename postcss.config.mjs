/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // For Tailwind 4 when ready
    "@tailwindcss/postcss": {},
    //tailwindcss: {},
  },
};

export default config;
